# Runtime dependencies. Keep these ranges aligned with pyproject.toml.
fastapi>=0.115,<1
uvicorn>=0.30,<1
psycopg[binary]>=3.2,<4
python-dotenv>=1.0,<2
requests>=2.32,<3
alembic>=1.13,<2
SQLAlchemy>=2.0,<3

# Install this project and expose the find-coffee command.
-e .
